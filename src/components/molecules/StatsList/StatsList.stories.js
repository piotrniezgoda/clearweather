import React from 'react';
import StatsList from './StatsList';

export default {
  title: 'StatsList',
  component: StatsList,
};

// eslint-disable-next-line import/prefer-default-export
export const Primary = () => <StatsList />;
