import React from 'react';
import StatsRow from './StatsRow';

export default {
  title: 'StatsRow',
  component: StatsRow,
};

// eslint-disable-next-line import/prefer-default-export
export const Primary = () => <StatsRow />;
