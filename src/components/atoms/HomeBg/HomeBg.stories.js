import React from 'react';
import HomeBg from './HomeBg';

export default {
  title: 'HomeBg',
  component: HomeBg,
};

// eslint-disable-next-line import/prefer-default-export
export const Primary = () => <HomeBg />;
