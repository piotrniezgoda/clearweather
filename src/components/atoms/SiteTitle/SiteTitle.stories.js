import React from 'react';
import SiteHeader from './SiteHeader';

export default {
  title: 'SiteHeader',
  component: SiteHeader,
};

// eslint-disable-next-line import/prefer-default-export
export const Primary = () => <SiteHeader>Clearweather</SiteHeader>;
